# ZZFilterPlugin v24

独立的 iOS 系统版本筛选插件。

## v24
- 保留状态页完整统计：UI Hook、UI 调用、处理商品、隐藏商品、网络拦截、修改响应、详情预取、详情命中。
- 保留“刷新 / 重置统计 / 关闭”。
- 删除状态页底部旧的 V14 运行时扫描说明文字。
- 保留系统版本范围筛选、结果列表、复制/打开商品链接。
- 商品标题显示识别到的 `iOS x.y.z` 系统版本。
- 显示标题继续去除“钛金属”“全网通”。

## 构建
使用仓库中的 GitHub Actions 编译即可。目标 iOS 16+，架构 arm64 / arm64e。
